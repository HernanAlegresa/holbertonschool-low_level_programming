#include <stdio.h>
/**
 * main- prints "Programming is like building a multilingual puzzle". It serves an entry point.
 *
 * return: is always 0.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

